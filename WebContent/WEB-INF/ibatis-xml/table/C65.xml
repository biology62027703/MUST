<?xml version="1.0" encoding="Big5"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">
<!--
  SO..C60 
  generated by SqlServerIbator on 2014-12-19 10:47:30.58
-->
<sqlMap namespace="C65">

	<select id="QueryByKey" resultClass="java.util.HashMap">
		select crtid
		from so..c65 with (nolock)
		<dynamic prepend="where">
			<isNotNull prepend="and" property="doc_no">
				doc_no = #doc_no#
			</isNotNull>
			<isNotNull prepend="and" property="rdcdt">
				rdcdt = #rdcdt#
			</isNotNull>
		</dynamic>
	</select>

	<update id="UpdateByKey" parameterClass="java.util.HashMap">
		update so..c65
		<dynamic prepend="set">
			<isNotNull prepend="," property="dpt">
				dpt = #dpt#
			</isNotNull>
			<isNotNull prepend="," property="crmdt">
				crmdt = #crmdt#
			</isNotNull>
			<isNotNull prepend="," property="pedt">
				pedt = #pedt#
			</isNotNull>
			<isNotNull prepend="," property="crmyy">
				crmyy = #crmyy#
			</isNotNull>
			<isNotNull prepend="," property="crmid">
				crmid = #crmid#
			</isNotNull>
			<isNotNull prepend="," property="crmno">
				crmno = #crmno#
			</isNotNull>			
		</dynamic>
		where doc_no = #doc_no# and rdcdt = #rdcdt#
	</update>

	<update id="InsertData" parameterClass="java.util.HashMap">
		insert into so..c65
			(crtid, sys, doc_no, crmyy, crmid, crmno, crmdt, dpt, pedt, rdcdt)
		values
			(#crtid#, #sys#, #doc_no#, #crmyy#, #crmid#, #crmno#, #crmdt#, #dpt#, #pedt#, #rdcdt#)
	</update>

</sqlMap>
