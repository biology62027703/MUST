<?xml version="1.0" encoding="Big5"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">
<!--
  SO..CITYAREA 
  generated by SqlServerIbator on 2014-12-19 10:47:30.926
-->
<sqlMap namespace="CITYAREA">

	<select id="queryAll" resultClass="com.sertek.table.CITYAREA">
		select rowid, zipcode, city, area
		from so..cityarea with (nolock)
	</select>

	<select id="queryByKey" resultClass="com.sertek.table.CITYAREA"  parameterClass="java.util.HashMap">
		select rowid, zipcode, city, area
		from so..cityarea with (nolock)
		<dynamic prepend="where">
			<isNotNull prepend="and" property="zipcode">
				zipcode = #zipcode#
			</isNotNull>
			<isNotNull prepend="and" property="city">
				city = #city#
			</isNotNull>
			<isNotNull prepend="and" property="area">
				area = #area#
			</isNotNull>
		</dynamic>
		<isNotNull prepend="" property="orderByKey">
			order by $orderByKey$
		</isNotNull>
	</select>

	<select id="queryByRowid" resultClass="com.sertek.table.CITYAREA"  parameterClass="java.util.HashMap">
		select rowid, zipcode, city, area
		from so..cityarea with (nolock)
		where rowid = #rowid#
	</select>

	<update id="updateByRowid" parameterClass="java.util.HashMap">
		update so..cityarea
		<dynamic prepend="set">
			<isNotNull prepend="," property="zipcode">
				zipcode = #zipcode#
			</isNotNull>
			<isNotNull prepend="," property="city">
				city = #city#
			</isNotNull>
			<isNotNull prepend="," property="area">
				area = #area#
			</isNotNull>
		</dynamic>
		where rowid = #rowid#
	</update>

	<delete id="deleteByRowid" parameterClass="java.util.HashMap">
		delete so..cityarea
		where rowid = #rowid#
	</delete>

	<delete id="deleteByKey" parameterClass="java.util.HashMap">
		delete so..cityarea
		<dynamic prepend="where">
			<isNotNull prepend="and" property="zipcode">
				zipcode = #zipcode#
			</isNotNull>
			<isNotNull prepend="and" property="city">
				city = #city#
			</isNotNull>
			<isNotNull prepend="and" property="area">
				area = #area#
			</isNotNull>
		</dynamic>
	</delete>

	<insert id="insert" parameterClass="com.sertek.table.CITYAREA">
		insert into so..cityarea
			(zipcode, city, area)
		values
			(#zipcode#, #city#, #area#)
	</insert>

	<insert id="insertSelective" parameterClass="java.util.HashMap">
		insert into so..cityarea
		<dynamic prepend="(" >
			<isNotNull prepend="," property="zipcode">
				zipcode
			</isNotNull>
			<isNotNull prepend="," property="city">
				city
			</isNotNull>
			<isNotNull prepend="," property="area">
				area
			</isNotNull>
			)
		</dynamic>
		values
		<dynamic prepend="(" >
			<isNotNull prepend="," property="zipcode">
				#zipcode#
			</isNotNull>
			<isNotNull prepend="," property="city">
				#city#
			</isNotNull>
			<isNotNull prepend="," property="area">
				#area#
			</isNotNull>
			)
		</dynamic>
	</insert>

	<insert id="cloneTable" parameterClass="java.util.HashMap">
		insert into $toTable$
			(zipcode, city, area)
		select zipcode, city, area
		from $fromTable$
		where crmyy = #crmyy#
		  and crmid = #crmid#
		  and crmno = #crmno#
	</insert>

</sqlMap>
